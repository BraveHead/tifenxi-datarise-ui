# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is the **DataRise Design System** repository — generated by **Claude Design** based on the `datarise-design-with-make` 项目，以「满意度分析」模块为标杆样例提炼而成。It contains design tokens and React + Tailwind components, with Storybook for local development and preview.

Design changes flow: `datarise-design-with-make` (Figma/Claude Design 上游) → this repo (via auto-PR workflow) → consumed by `ti-datarise-frontend`.

## Commands

```bash
yarn dev              # Storybook dev server (port 6006)
yarn build            # Vite library build → dist/
yarn build:storybook  # Storybook static build → storybook-static/
```

## Structure

```
src/design-system/
  tokens.css           # :root CSS custom properties — 设计变量唯一源
  styles.css           # Tailwind v4 入口 + @theme inline 映射 tokens → Tailwind utilities
  index.ts             # Barrel export
  components/          # React + Tailwind 组件（每组件一个 .tsx + .stories.tsx）
    Button.tsx
    Button.stories.tsx
docs/
  design-system.html   # 离线可打开的完整规范手册
.storybook/            # Storybook 8 配置
vite.config.ts         # Vite library mode + Tailwind v4 plugin
```

## Architecture & Design Decisions

### Styling: Tailwind Only

**All components use Tailwind utility classes exclusively.** No standalone `.css` component files.

Token flow: `tokens.css` (`:root` CSS variables) → `styles.css` (`@theme inline` mapping) → Tailwind utility classes in components.

### Token System

All visual values are CSS custom properties on `:root` in `tokens.css`. The naming convention:

- **Brand**: `--brand-{50..700}` — primary/accent blue
- **Neutral**: `--neutral-{0..900}` — backgrounds, text, borders
- **Semantic**: `--{success,warning,danger,info}-{bg,border,500,700}` — 4-tier per semantic color
- **Chart**: `--chart-{1..5}` for satisfaction scores, `--series-{1..6}` for multi-line/bar charts
- **Spacing**: `--sp-{0..16}` on a 4px grid
- **Radii**: `--radius-{xs..full}`

In `styles.css`, these are mapped via `@theme inline` to Tailwind v4, e.g. `--brand-500` → `--color-brand-500` → `bg-brand-500`.

### Semantic Color Rules for Trends

- **Higher-is-better metrics** (satisfaction rate): ↑ green (`success-*`), ↓ red (`danger-*`)
- **Lower-is-better metrics** (complaint rate): ↑ red (`danger-*`), ↓ green (`success-*`)

### Module Composition Standard

Module slot order is fixed: `Header → KPI Row → Segment Tabs → AI 总结 → Section × N → Action List`.

### Adding a New Component

1. Create `src/design-system/components/MyComponent.tsx` — Tailwind classes only
2. Create `src/design-system/components/MyComponent.stories.tsx` — Storybook CSF3
3. Export from `src/design-system/index.ts`
4. Run `yarn dev` to verify in Storybook

## Consuming This Design System

Currently via **monorepo direct import** from `ti-datarise-frontend`:

1. Import `styles.css` as global styles (includes Tailwind + token mapping)
2. Import components from `src/design-system/components/`

## GitHub Actions Workflows

| Workflow | Trigger | Purpose |
|---|---|---|
| `claude-design-auto-pr.yml` | `workflow_dispatch` | Sync design assets from Claude Design → auto PR |
| `storybook-deploy.yml` | push to `main` | Build & deploy Storybook → GitHub Pages |
